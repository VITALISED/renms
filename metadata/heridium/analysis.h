#pragma once

#include "framework.h"

//HACK: since I lack the braincells to figure out how hashes are generated we will instead retrieve them from the generated functions.
constexpr LPVOID Heridium_Analysis_UnpackClassPointerWriteFunc(void (*ClassPointerWriteFunction)(const cTkClassPointer*, XMLNode*, bool))
{

}