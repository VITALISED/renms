if(DEFINED EXAMPLE_BUILD_TYPE)
    if(${EXAMPLE_BUILD_TYPE} MATCHES "simple_plugin")
        add_subdirectory(simple_plugin)
    else()
        message(FATAL_ERROR "No example target found!")
    endif()
endif()
